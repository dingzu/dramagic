# 🚀 Dramagic 快速开始指南

## 欢迎使用 Dramagic！

这是一个完整的前后端分离项目，所有基础设施已经就绪，现在你可以开始开发了！

## 📋 项目已完成内容

✅ Vue 3 前端工程（Vercel 部署配置）  
✅ Node.js + Express 后端工程（Railway 部署配置）  
✅ 完整的文档系统（产品、开发、API、部署）  
✅ Cursor 开发规范（.cursorrules）  
✅ 环境变量配置  
✅ 所有依赖已安装  

## 🎯 快速启动

### 1️⃣ 启动后端服务

```bash
cd backend
npm run dev
```

访问：http://localhost:3000

测试接口：
- 健康检查：http://localhost:3000/health
- API 信息：http://localhost:3000/
- 示例接口：http://localhost:3000/api/v1/example

### 2️⃣ 启动前端服务

在新的终端窗口中：

```bash
cd frontend
npm run dev
```

访问：http://localhost:5173

## 📚 重要文档

| 文档 | 路径 | 说明 |
|------|------|------|
| 项目总结 | `PROJECT_SUMMARY.md` | 完整的项目概览和已完成工作 |
| 开发文档 | `docs/development.md` | **必读**！记录所有开发细节 |
| 产品文档 | `docs/product.md` | 产品定位和功能说明 |
| API 文档 | `docs/api.md` | 后端接口规范 |
| 部署文档 | `docs/deployment.md` | Vercel 和 Railway 部署指南 |
| 开发规范 | `.cursorrules` | **核心规范**！必须遵守 |

## ⚠️ 核心开发规范

### 第一条规范（最重要！）

**所有开发内容都必须更新到 `docs/development.md` 中！**

- 新增功能 → 更新文档 ✍️
- 修改功能 → 更新文档 ✍️
- 删除功能 → 更新文档 ✍️
- Bug 修复 → 更新文档 ✍️

### 提交规范

使用语义化提交信息：

```bash
git commit -m "feat: 添加用户登录功能"
git commit -m "fix: 修复登录接口错误"
git commit -m "docs: 更新 API 文档"
```

## 🔧 环境配置

### 前端环境变量

文件：`frontend/.env`

```env
VITE_API_BASE_URL=http://localhost:3000
VITE_APP_ENV=development
```

### 后端环境变量

文件：`backend/.env`

```env
PORT=3000
NODE_ENV=development
API_VERSION=v1
```

## 📦 项目结构

```
dramagic/
├── .cursorrules           # 开发规范（必读）
├── PROJECT_SUMMARY.md     # 项目总结
├── QUICK_START.md         # 快速开始（本文件）
├── README.md              # 项目说明
│
├── frontend/              # Vue 3 前端
│   ├── src/              # 源代码
│   ├── public/           # 静态资源
│   ├── .env              # 环境变量
│   ├── vercel.json       # Vercel 配置
│   └── package.json      # 依赖配置
│
├── backend/               # Node.js 后端
│   ├── src/
│   │   └── index.js      # 主入口（含所有路由）
│   ├── .env              # 环境变量
│   ├── railway.json      # Railway 配置
│   └── package.json      # 依赖配置
│
└── docs/                  # 项目文档
    ├── product.md        # 产品文档
    ├── development.md    # 开发文档（核心）
    ├── api.md            # API 文档
    └── deployment.md     # 部署文档
```

## 🎨 前端开发

### 推荐安装的依赖

```bash
cd frontend

# 路由
npm install vue-router

# 状态管理
npm install pinia

# UI 组件库（选其一）
npm install element-plus
# 或
npm install ant-design-vue

# HTTP 请求
npm install axios
```

### 推荐的项目结构

```
frontend/src/
├── api/           # API 接口
├── assets/        # 静态资源
├── components/    # 公共组件
├── router/        # 路由配置
├── stores/        # Pinia 状态管理
├── views/         # 页面组件
├── utils/         # 工具函数
├── App.vue        # 根组件
└── main.js        # 入口文件
```

## 🔧 后端开发

### 推荐安装的依赖

```bash
cd backend

# 数据库（选其一）
npm install mongoose        # MongoDB
npm install mysql2          # MySQL
npm install pg              # PostgreSQL

# 身份认证
npm install jsonwebtoken bcrypt

# 数据验证
npm install joi

# 日志
npm install winston
```

### 推荐的项目结构

```
backend/src/
├── controllers/   # 控制器
├── models/        # 数据模型
├── routes/        # 路由
├── middlewares/   # 中间件
├── utils/         # 工具函数
├── config/        # 配置文件
└── index.js       # 入口文件
```

## 🚀 部署到生产环境

### 前端部署到 Vercel

1. 访问 https://vercel.com
2. 连接 GitHub 仓库
3. 选择 `frontend` 目录
4. 配置环境变量：
   ```
   VITE_API_BASE_URL=https://your-backend.railway.app
   ```
5. 点击部署

### 后端部署到 Railway

1. 访问 https://railway.app
2. 新建项目
3. 从 GitHub 导入
4. 选择 `backend` 目录
5. 配置环境变量：
   ```
   PORT=8080
   NODE_ENV=production
   ```
6. 自动部署

## 🐛 常见问题

### Q: 前端无法连接后端？
A: 检查后端是否已启动，确认 `VITE_API_BASE_URL` 配置正确。

### Q: 端口被占用？
A: 修改 `backend/.env` 中的 `PORT` 值，或关闭占用端口的进程。

### Q: 依赖安装失败？
A: 尝试删除 `node_modules` 和 `package-lock.json`，然后重新运行 `npm install`。

## 📞 需要帮助？

- 查看开发文档：`docs/development.md`
- 查看 API 文档：`docs/api.md`
- 查看部署文档：`docs/deployment.md`

## 🎉 开始开发吧！

所有准备工作已完成，现在开始构建你的应用功能吧！

**记住**：每次开发都要更新文档！📝

---

祝你开发愉快！🚀
